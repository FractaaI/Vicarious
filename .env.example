# Vicarious does not require environment variables for normal local development.
#
# Optional: set this to true if renderer hot module replacement causes issues
# while running `npm run dev`.
DISABLE_HMR=false
